# 用户级体验验收

2026-08-14 的验收从普通用户黄金路径出发，不再把页面渲染、Schema 存在或 fixture 测试视为产品完成。

## 已实际验证

- 桌面端登录后直接进入自然语言旅行对话，不要求手工添加行程。
- 真实 DeepSeek 连续理解两轮输入：首次保存“大理、国庆、与父母同行、5 天”，第二轮把“广州、飞机、住宿位置由助手设计”合并到同一趟旅行，没有重复追问。
- “我和父母”在真实浏览器中显示为 3 位同行人；首次工具调用若保存了同行关系却遗漏可推断人数，会被拒绝并要求模型自行修正。
- 没有高德 Key 时，聊天和方案区都明确说明暂时无法取得实时地点资料，不显示固定候选或内部错误码；输入、对话和旅行理解在失败后仍可继续。
- 桌面端同时呈现对话与方案区；390×844 移动端按“对话 → 输入 → 方案”纵向排列，无横向溢出，方案摘要和恢复动作可达。
- 本地数据目录同时保存旅行和其对话，API 重启后不会出现旧对话指向另一数据目录的断链。

## 已实现但尚未真实数据验收

- 高德地点检索会归一照片、坐标、地址和导航链接；服务端静态地图接口不会把 Key 发送到前端。
- 有真实地点结果时，方案区展示每域候选照片、地点分布、来源、核验时间和用户选择；当前只通过合同测试，因为本机没有 `AMAP_API_KEY`。
- Kimi 图片理解接口存在，但现有密钥真实请求仍返回认证失败，界面尚不开放图片上传入口。

## 当前不能宣称完成

- 飞猪/铁路/酒店实时价格、库存与交易跳转。
- 小红书、抖音只读发现与微信文章读取。
- 公共交通实时路线、入口出口、电梯、卫生间、储物柜和充电宝。
- 微信、支付宝、Apple 与邮箱生产登录。

这些能力必须在账号、条款与真实数据接通后进行同层验收，fixture 只能证明接口和界面不会失控。
