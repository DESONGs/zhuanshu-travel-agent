# research/archive/

已关闭迭代的研究与审计记录。这里的文档**不再修订**，其结论已吸收进 `wiki/current/`；引用它们是为了追溯决策证据，而不是当前开发依据。

约定：

- 进行中的迭代文档放在 `wiki/research/` 根目录，根目录同时只保留一期迭代；
- 迭代关闭并完成验收后，文档连同 `assets/` 中对应截图目录一起移入本目录；
- 移入后同步更新 `wiki/README.md` 的历史研究归档表，并修正指向被移动文档的链接；
- 文件名日期前缀保持不变，按时间检索。
